*jQuery

<hasclass>
기준 요소에 해당 클래스가 있으면 true, 없으면 false 반환

<remove+empty>
remove()
기준 요소부터 하위 노드 전부 제거. 기준 요소도 제거
empty()
기준 요소의 하위 노드만 제거. 기준 요소는 남음

<prependTo+appendTo>

$('B').prependTo('A');
부모인 A의 자식들 중 요소 B를 자식들 중 제일 처음으로 *이동*

$('B').appendTo('A');
부모인 A의 자식들 중 요소 B를 자식들 중 제일 마지막으로 *이동*
